name: Long Sleep

on:
  workflow_dispatch:
jobs:
  SleepForLong:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Run a one-line script
        run: ./Sleep8hours.sh
